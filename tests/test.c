int main()
{
    while (i<43){
        if (a==1) 
            b+=2;
        else
            b+=3;
    }
    while (i<43)
        if (a==1) 
            b+=2;
        else
            b+=3;
    
    while (i<43);
    for (int i=0;i<n;i++){
        if (a==1) 
            b+=2;
        else
            b+=3;
    }
    for (int i=0;i<n;i++);
    return 0;
}
